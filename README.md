# Personal projet in order to learn Ruby on Rails

## Versions
* Ruby version : `ruby2.7.8`
* Rails version : `5.1.6`

## Setup
* Checkout project
* Run `bundle install`
* Run `rake db:setup`
* Run `rake db:migrate`
* Start server `rails server`
